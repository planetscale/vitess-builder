diff --git a/examples/compose/docker-compose.beginners.yml b/examples/compose/docker-compose.beginners.yml
index d01532c2f7..7bc85fea7a 100644
--- a/examples/compose/docker-compose.beginners.yml
+++ b/examples/compose/docker-compose.beginners.yml
@@ -94,6 +94,7 @@ services:
         -port $WEB_PORT \
         -grpc_port $GRPC_PORT \
         -mysql_server_port $MYSQL_PORT \
+        -mysql_server_version $MYSQL_SERVER_VERSION \
         -mysql_auth_server_impl none \
         -cell $CELL \
         -cells_to_watch $CELL \
@@ -101,8 +102,6 @@ services:
         -service_map 'grpc-vtgateservice' \
         -enable_system_settings=true \
         "]
-    volumes:
-      - ".:/script"
     environment:
       - KEYSPACE
       - DB
@@ -130,8 +129,6 @@ services:
     - SCHEMA_FILES
     - POST_LOAD_FILE
     - EXTERNAL_DB
-    volumes:
-    - .:/script
     depends_on:
       vttablet101:
         condition: service_healthy
@@ -143,7 +140,6 @@ services:
       - "$GRPC_PORT"
       - "3306"
     volumes:
-      - ".:/script"
       - "./backups:/vt/vtdataroot/backups"
     environment:
       - TOPOLOGY_FLAGS
@@ -175,7 +171,6 @@ services:
       - "$GRPC_PORT"
       - "3306"
     volumes:
-      - ".:/script"
       - "./backups:/vt/vtdataroot/backups"
     environment:
       - TOPOLOGY_FLAGS
@@ -207,7 +202,6 @@ services:
       - "$GRPC_PORT"
       - "3306"
     volumes:
-      - ".:/script"
       - "./backups:/vt/vtdataroot/backups"
     environment:
       - TOPOLOGY_FLAGS
@@ -239,7 +233,6 @@ services:
       - "$GRPC_PORT"
       - "3306"
     volumes:
-      - ".:/script"
       - "./backups:/vt/vtdataroot/backups"
     environment:
       - TOPOLOGY_FLAGS
@@ -271,8 +264,6 @@ services:
       - vtctld
     ports:
       - "13000:3000"
-    volumes:
-      - ".:/script"
     environment:
       - TOPOLOGY_FLAGS
       - WEB_PORT
@@ -294,8 +285,6 @@ services:
 
   vreplication:
     image: vitess/lite:${VITESS_TAG:-latest}
-    volumes:
-      - ".:/script"
     environment:
       - TOPOLOGY_FLAGS
       - WEB_PORT
diff --git a/examples/compose/external_db/mysql/Dockerfile b/examples/compose/external_db/mysql/Dockerfile
index f44c63951e..3bf6d87281 100644
--- a/examples/compose/external_db/mysql/Dockerfile
+++ b/examples/compose/external_db/mysql/Dockerfile
@@ -1,2 +1,2 @@
-FROM mysql:5.7
-COPY . /docker-entrypoint-initdb.d
\ No newline at end of file
+FROM mysql:latest
+COPY . /docker-entrypoint-initdb.d
